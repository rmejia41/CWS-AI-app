services:
  - type: web
    name: CWS-AI-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python src/app.py"
    envVars:
      - key: OPENAI_API_KEY
        value: YOUR_OPENAI_API_KEY

